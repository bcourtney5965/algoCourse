type Point = {
    x: number;
    y: number;
};

const walk = (
    start: Point,
    end: Point,
    path: Point[],
    wall: string,
): boolean => {
    // BASE CASES
    // if at the end
    // if outside
    // if at a wall
    // if already visited
    // RECURSIVE CASE
    return false;
};

export default function solve(
    maze: string[],
    wall: string,
    start: Point,
    end: Point,
): Point[] {}
