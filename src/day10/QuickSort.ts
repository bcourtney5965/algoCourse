const qs = (arr: number[], lo: number, high: number): void => {};

const partition = (arr: number[], lo: number, high: number): number => {};

export default function quick_sort(arr: number[]): void {
    qs(arr, 0, arr.length - 1);
}
